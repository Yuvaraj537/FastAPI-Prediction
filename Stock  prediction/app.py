
<p class="muted">
      Upload a CSV with <code>Date</code> and <code>Close</code> columns (or use the fallback
      <code>coin_Bitcoin.csv</code> in the project root). We log-transform, train Prophet, forecast,
      inverse transform, compute metrics, and plot results.
    </p>